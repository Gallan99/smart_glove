# Legacy single-channel GUI

`gui_script_v2.py` is the original desktop logger supplied with the project. It expects a six-field, single-channel serial row and is retained for implementation traceability.

Use `smart-glove-monitor` for the current 26-field, synchronized five-channel firmware. The two interfaces are intentionally separated because their serial formats are not interchangeable.
