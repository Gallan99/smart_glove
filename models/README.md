# Generated models

Run `smart-glove-train` to create `random_forest.joblib` in this directory.

Serialized model files are intentionally excluded from version control because they can be reproduced from the committed dataset. Only load model files produced by a trusted source.
